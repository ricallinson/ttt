package main

import (
	// "github.com/ricallinson/booli"
	. "github.com/ricallinson/simplebdd"
	"testing"
	// "os"
)

func TestTtt(t *testing.T) {

	// var input []*booli.Booli
	// var output []*booli.Booli

	// BeforeEach(func() {
	// 	input = []*booli.Booli{
	// 		booli.New(),
	// 	}

	// 	output = []*booli.Booli{
	// 		booli.New(),
	// 		booli.New(),
	// 		booli.New(),
	// 		booli.New(),
	// 		booli.New(),
	// 		booli.New(),
	// 		booli.New(),
	// 		booli.New(),
	// 		booli.New(),
	// 	}
	// })

	Describe("", func() {
		It("should ", func() {
			// loop(os.Stdin, os.Stdout, getBoard(), getWins())
		})
	})

	Report(t)
}
